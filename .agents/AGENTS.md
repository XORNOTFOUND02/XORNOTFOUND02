# Workspace Rules: XORNOTFOUND02 Git Synchronization

Whenever changes are made or new features are implemented in this workspace:
1. Always stage the modified files: `git add .`
2. Commit the changes with a concise commit message detailing the updates.
3. Push the commits to the remote: `git push origin main`
4. Keep the repository synchronized with the local changes at all times.
